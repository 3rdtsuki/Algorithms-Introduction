与&，或|，非~，异或^
加法：
int add(int a,int b){
	int ans=0;
	while(b){
		ans=a^b;	//ans是没进位的结果
		b=(a&b)<<1;	//假如有进位，该位a&b必为1，所以左移这个1相当于进位了（注：一步会有多位用到这点）
		a=ans;		//没进位的结果参加下一次的进位运算，直到不再产生任何进位为止
	}
	return ans;
}
